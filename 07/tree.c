#include "tree.h"
#include "kvec.h"

struct tree {
    char *data;
    kvec(struct tree);
};

tree_t tree_node(char *data, tree_t* children, size_t size) {
    size_t i;
    kvec(tree_t) vector
    for (i = 0; i < size; i++) {

    }
}
